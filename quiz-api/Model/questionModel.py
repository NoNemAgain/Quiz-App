class QuestionModel(object):
        def __init__(self, position, title, text, image):
                self.position =position
                self.title = title
                self.text =text
                self.image = image

